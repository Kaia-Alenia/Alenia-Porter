# Alenia Porter Frontend

React/Vite interface used by the pywebview desktop shell.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
