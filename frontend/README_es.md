# Alenia Porter Frontend

Interfaz de React/Vite utilizada por el shell de escritorio pywebview.

## Ejecución Local

**Requisitos previos:** Node.js

1. Instalar dependencias:
   `npm install`
2. Ejecutar la aplicación:
   `npm run dev`
